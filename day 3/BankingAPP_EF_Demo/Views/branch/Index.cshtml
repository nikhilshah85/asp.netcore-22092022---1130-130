@model IEnumerable<BankingAPP_EF_Demo.Models.EF.BranchInfo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BranchName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BranchCity)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BranchName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BranchCity)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BranchNo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BranchNo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BranchNo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
